{
  "name": "ai-learning-knowledge-graph",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev:backend": "cd backend && python -m uvicorn app.main:app --reload",
    "dev:frontend": "npm --prefix frontend run dev",
    "test:backend": "cd backend && pytest",
    "test:frontend": "npm --prefix frontend test -- --run",
    "test:desktop": "npm --prefix desktop test",
    "build:frontend": "npm --prefix frontend run build",
    "build:backend:desktop": "cd backend && pyinstaller pyinstaller.spec",
    "build:desktop": "npm run build:frontend && npm --prefix desktop run build",
    "build:desktop:win": "node -e \"if (process.platform !== 'win32') { console.error('build:desktop:win must run on Windows because the PyInstaller backend sidecar is platform-specific. Use the GitHub Actions Windows Desktop Package workflow.'); process.exit(1); }\" && npm run build:frontend && npm run build:backend:desktop && npm --prefix desktop run dist:win",
    "dev:desktop": "npm run build:frontend && npm --prefix desktop run dev",
    "verify:packaging": "./scripts/verify-packaging.sh"
  }
}
